version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        # Amplify automatically detects SSR and handles Lambda deployment
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*